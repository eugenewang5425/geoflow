"use strict";
const $ = id => document.getElementById(id);
const fmt = n => Number(n).toLocaleString("zh-CN");
const fmtk = n => n >= 1000 ? (n / 1000).toFixed(1) + " 千" : fmt(Math.round(n));
let geo, current, generation = 0, selectedZone = null, pollTimer;
const paths = new Map();
const charts = {};
const escapeHtml = value => String(value).replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
function notice(message, error) { const n = $("notice"); n.hidden = !message; n.textContent = message; n.classList.toggle("error", !!error); }
async function get(url, options) { const r = await fetch(url, options); if (!r.ok) { let info; try { info = (await r.json()).detail; } catch { info = r.statusText; } throw Error(typeof info === "string" ? info : JSON.stringify(info)); } return r.json(); }
function params() { const q = new URLSearchParams(); if (+$("hour").value < 24) q.set("hour", $("hour").value); if ($("borough").value) q.set("borough", $("borough").value); if ($("month").value) q.set("month", $("month").value); return q.toString(); }
function project(coord) { return [coord[0] * Math.PI / 180, -Math.log(Math.tan(Math.PI / 4 + coord[1] * Math.PI / 360))]; }
function chart(id) { if (!charts[id]) { charts[id] = echarts.init($(id)); } return charts[id]; }
function resizeCharts() { Object.values(charts).forEach(c => c && c.resize()); }
const palette = { blue: "#4c7dd8", purple: "#8b6fd8", teal: "#2fa08f", amber: "#e0a33e", red: "#d8584c", gray: "#9aa7b5" };
function openZoom(id) { if (!charts[id]) return; const overlay = document.createElement("div"); overlay.className = "zoom-overlay"; const card = document.createElement("div"); card.className = "zoom-card"; const head = document.createElement("div"); head.className = "zoom-head"; const title = document.createElement("strong"); title.textContent = "放大查看"; const btn = document.createElement("button"); btn.textContent = "✕ 关闭"; const box = document.createElement("div"); box.className = "zoom-box"; head.appendChild(title); head.appendChild(btn); card.appendChild(head); card.appendChild(box); overlay.appendChild(card); document.body.appendChild(overlay); const zc = echarts.init(box); zc.setOption(charts[id].getOption()); const closer = () => { zc.dispose(); overlay.remove(); window.removeEventListener("resize", onResize); }; const onResize = () => zc.resize(); btn.onclick = closer; overlay.onclick = e => { if (e.target === overlay) closer(); }; window.addEventListener("resize", onResize); }
function attachZoom() { document.querySelectorAll(".chart").forEach(el => { if (el.dataset.zoom) return; el.dataset.zoom = "1"; const ph = el.closest ? el.closest(".panel") : null; const heading = ph ? ph.querySelector(".panel-heading") : null; if (!heading) return; const b = document.createElement("button"); b.className = "zoom-btn"; b.textContent = "⤢"; b.title = "点击放大查看"; b.onclick = e => { e.stopPropagation(); openZoom(el.id); }; heading.appendChild(b); }); }
function zoneDirs(g) { const all = []; g.features.forEach(f => { const polys = f.geometry.type === "Polygon" ? [f.geometry.coordinates] : f.geometry.coordinates; polys.forEach(p => p.forEach(r => r.forEach(c => all.push(project(c))))); }); const xs = all.map(p => p[0]), ys = all.map(p => p[1]); const minX = Math.min(...xs), maxX = Math.max(...xs), minY = Math.min(...ys), maxY = Math.max(...ys); const scale = Math.min(710 / (maxX - minX), 440 / (maxY - minY)); return { scale, minX, maxX, minY, maxY }; }
function zonePath(f, dirs) { const polys = f.geometry.type === "Polygon" ? [f.geometry.coordinates] : f.geometry.coordinates; const xy = c => { const q = project(c); return [400 + (q[0] - (dirs.minX + dirs.maxX) / 2) * dirs.scale, 256 + (q[1] - (dirs.minY + dirs.maxY) / 2) * dirs.scale]; }; return polys.map(p => p.map(r => r.map((c, i) => (i ? "L" : "M") + xy(c).map(v => v.toFixed(2)).join(",")).join("") + "Z").join("")).join(""); }
function zoneCenter(f) { const q = f.geometry.type === "Polygon" ? f.geometry.coordinates[0] : f.geometry.coordinates[0][0]; let x = 0, y = 0; q.forEach(c => { x += c[0]; y += c[1]; }); return [x / q.length, y / q.length]; }
function nameOfGeo(zid) { const n = Number(zid); const f = geo.features.find(x => x.properties.id === n); return f ? f.properties.name : String(zid); }

/* ========== overview ========== */
function buildMap() { const dirs = zoneDirs(geo); for (const f of geo.features) { const path = document.createElementNS("http://www.w3.org/2000/svg", "path"); path.setAttribute("d", zonePath(f, dirs)); path.setAttribute("fill", "#d4dfeb"); path.setAttribute("fill-rule", "evenodd"); path.dataset.id = f.properties.id; path.setAttribute("tabindex", "0"); path.setAttribute("role", "button"); path.setAttribute("aria-label", f.properties.name); const title = document.createElementNS("http://www.w3.org/2000/svg", "title"); title.textContent = f.properties.name; path.appendChild(title); path.addEventListener("click", () => selectZone(f.properties.id)); path.addEventListener("keydown", e => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); selectZone(f.properties.id); } }); $("map").appendChild(path); if (!paths.has(f.properties.id)) paths.set(f.properties.id, []); paths.get(f.properties.id).push(path); } }
function selectZone(id) { selectedZone = id; paths.forEach((parts, z) => parts.forEach(x => x.classList.toggle("selected", z === id))); const zone = current ? current.zones.find(z => z.id === id) : null; const info = geo.features.find(f => f.properties.id === id); $("zone-detail").replaceChildren(); const name = document.createElement("strong"), desc = document.createElement("span"); name.textContent = (zone && zone.name) || (info ? info.properties.name : "未知区域"); desc.textContent = zone ? fmt(zone.trips) + " 次出行 · 平均 " + zone.avg_distance_km.toFixed(2) + " km" : "当前筛选条件下无有效记录"; $("zone-detail").append(name, desc); }
async function refreshAnalysis() { const request = ++generation; try { const data = await get("/api/analysis?" + params()); if (request !== generation) return; current = data; const m = data.summary, quality = data.quality; $("kpi-trips").textContent = fmt(m.trips); $("kpi-distance").innerHTML = m.avg_distance_km.toFixed(2) + "<em>km</em>"; $("kpi-duration").innerHTML = m.avg_minutes.toFixed(1) + "<em>min</em>"; $("kpi-quality").innerHTML = (100 * quality.valid / quality.input).toFixed(2) + "<em>%</em>"; $("dataset-month").textContent = data.month.replace("-", " 年 ") + " 月 · 整月记录"; $("export").href = "/api/export?" + params(); const max = Math.max(1, ...data.zones.map(z => z.trips)); const byId = new Map(data.zones.map(z => [z.id, z])); paths.forEach((parts, id) => { const n = byId.get(id) ? byId.get(id).trips : 0; const t = Math.log1p(n) / Math.log1p(max); const fill = n ? "rgb(" + Math.round(218 - 184 * t) + "," + Math.round(234 - 149 * t) + "," + Math.round(250 - 91 * t) + ")" : "#dce4ec"; parts.forEach(x => x.setAttribute("fill", fill)); }); const rank = $("ranking"); rank.replaceChildren(); data.zones.slice(0, 8).forEach((z, i) => { const row = document.createElement("div"); row.className = "rank-row"; row.tabIndex = 0; row.setAttribute("role", "button"); row.setAttribute("aria-label", "查看 " + z.name + " · " + z.trips + " 次"); row.innerHTML = "<span class=\"rank-num\">" + String(i + 1).padStart(2, "0") + "</span><div><div class=\"rank-name\">" + escapeHtml(z.name) + "</div><div class=\"rank-bar\"><span style=\"width:" + (100 * z.trips / max) + "%\"></span></div></div><span class=\"rank-value\">" + fmt(z.trips) + "</span>"; row.onclick = () => selectZone(z.id); row.onkeydown = e => { if (e.key === "Enter") selectZone(z.id); }; rank.appendChild(row); }); if (!data.zones.length) rank.textContent = "当前筛选没有记录。"; const top = data.zones[0]; $("insight").textContent = top ? top.name + " 在当前筛选中出行最多，占有效上车记录的 " + (100 * top.trips / m.trips).toFixed(1) + "%。该值反映记录中的需求，不能直接解释为拥堵程度。" : "请更换区域或时段，查看空间分布。"; const hc = $("hour-chart"); hc.replaceChildren(); const peak = Math.max(1, ...data.hourly); data.hourly.forEach((n, h) => { const b = document.createElement("button"); b.style.height = Math.max(2, n / peak * 100) + "%"; if (n === peak) b.className = "peak"; b.title = h + ":00 · " + fmt(n) + " 次"; b.setAttribute("aria-label", b.title); b.onclick = () => { $("hour").value = h; updateHour(); refreshAnalysis(); }; hc.appendChild(b); }); chart("trend").setOption({ grid: { left: 44, right: 16, top: 20, bottom: 26 }, xAxis: { type: "category", data: data.hourly.map((n, h) => h + ":00"), axisLabel: { interval: 3 } }, yAxis: { type: "value", splitLine: { lineStyle: { color: "#eef2f7" } } }, series: [{ type: "line", smooth: true, symbol: "none", data: data.hourly, lineStyle: { color: palette.blue, width: 2.5 }, areaStyle: { color: "rgba(76,125,216,0.12)" } }] }); $("quality").innerHTML = "<div class=\"quality-row\"><span>原始记录</span><strong>" + fmt(quality.input) + "</strong></div><div class=\"quality-row\"><span>有效记录</span><strong>" + fmt(quality.valid) + "</strong></div><div class=\"quality-track\"><span style=\"width:" + (quality.valid / quality.input * 100) + "%\"></span></div><div class=\"quality-row\"><span>规则排除</span><strong>" + fmt(quality.rejected) + "</strong></div><div class=\"quality-row\"><span>区域 / 日期 / OD / 日粒度计数守恒</span><strong>已通过 ✓</strong></div>"; if (selectedZone !== null) selectZone(selectedZone); } catch (e) { notice(e.message, true); } }
function updateHour() { const h = +$("hour").value; const caption = h === 24 ? "全天累计" : String(h).padStart(2, "0") + ":00–" + String(h).padStart(2, "0") + ":59"; $("hour-caption").textContent = caption; $("filter-caption").textContent = caption; }
async function refreshCluster() { try { const c = await get("/api/cluster"); $("side-dot").classList.toggle("online", c.online); $("side-state").textContent = c.online ? (c.yarn.activeNodes + " 个计算节点在线") : "集群离线 · 可浏览历史结果"; $("cluster").innerHTML = "<div class=\"cluster-item\"><span>HDFS DataNodes</span><strong>" + (c.hdfs ? c.hdfs.NumLiveDataNodes : "—") + "</strong></div><div class=\"cluster-item\"><span>YARN NodeManagers</span><strong>" + (c.yarn ? c.yarn.activeNodes : "—") + "</strong></div><div class=\"cluster-item\"><span>HDFS 数据块</span><strong>" + (c.hdfs ? fmt(c.hdfs.BlocksTotal) : "—") + "</strong></div>" + c.nodes.map(n => "<div class=\"cluster-item\"><span>" + escapeHtml(n.id) + "</span><strong>" + n.numContainers + " 容器</strong></div>").join(""); } catch { $("side-state").textContent = "暂时无法读取集群状态"; } }
function refreshRuns() { return get("/api/runs").then(data => { $("runs").innerHTML = "<thead><tr><th>作业 ID</th><th>状态</th><th>Reducer</th><th>Combiner</th><th>耗时</th></tr></thead><tbody>" + data.runs.map(r => "<tr><td>" + escapeHtml(r.run_id) + "<small>" + escapeHtml(r.job_id || "尚无作业 ID") + "</small></td><td><span class=\"status " + escapeHtml(r.status) + "\">" + escapeHtml(r.status) + "</span></td><td>" + r.reducers + "</td><td>" + (r.combiner ? "开启" : "关闭") + "</td><td>" + (r.elapsed_seconds != null ? r.elapsed_seconds + " s" : "—") + "</td></tr>").join("") + "</tbody>"; return data.current; }); }
async function refreshEvidence() { const e = await get("/api/evidence"); const v = e.verification; $("evidence").innerHTML = v ? "<div class=\"evidence-grid\"><div class=\"evidence-card\"><strong>" + (v.exact_match ? "完全一致" : "存在差异") + "</strong><span>单进程参考结果 vs Hadoop 输出</span></div><div class=\"evidence-card\"><strong>" + fmt(v.compared_keys) + "</strong><span>逐键核对的聚合结果</span></div><div class=\"evidence-card\"><strong>" + fmt(v.input_rows) + "</strong><span>真实输入行数</span></div></div>" : "<p class=\"muted\">对照实验尚未运行；不展示推测结果。</p>"; $("exc-exact").textContent = v ? ("核对 " + fmt(v.compared_keys) + " 键") : "—"; }
document.querySelectorAll(".nav").forEach(button => button.onclick = async () => { document.querySelectorAll(".view").forEach(v => v.hidden = v.id !== button.dataset.view); document.querySelectorAll(".nav").forEach(b => b.classList.toggle("active", b === button)); try { if (button.dataset.view === "experiments") await Promise.all([refreshRuns(), refreshEvidence()]); if (button.dataset.view === "architecture") await refreshCluster(); if (button.dataset.view === "year") await renderYear(); if (button.dataset.view === "od") await renderOd(); if (button.dataset.view === "d3") await renderD3(); if (button.dataset.view === "forecast") await renderForecast(); attachZoom(); resizeCharts(); } catch (e) { notice(e.message, true); } });
$("hour").addEventListener("input", updateHour); $("hour").addEventListener("change", refreshAnalysis); $("borough").addEventListener("change", refreshAnalysis); $("month").addEventListener("change", refreshAnalysis); $("d3-month").addEventListener("change", () => renderD3().catch(e => notice(e.message, true))); $("d3-sync").addEventListener("change", () => setGLView($("d3-sync").checked)); $("d3-alpha").addEventListener("input", () => setGLView($("d3-sync").checked)); $("d3-beta").addEventListener("input", () => setGLView($("d3-sync").checked)); $("d3-dist").addEventListener("input", () => setGLView($("d3-sync").checked)); $("d3-preset").addEventListener("change", () => d3Preset());
["d3-layer-dem", "d3-layer-towers"].forEach(id => { $(id).addEventListener("change", () => { if (charts["chart-scatter3d"]) charts["chart-scatter3d"].setOption({ series: [{ id: "dem", show: $("d3-layer-dem").checked }, { id: "towers", show: $("d3-layer-towers").checked }] }); }); });
$("d3-layer-flow").addEventListener("change", () => { if (charts["chart-flow3d"]) { const flowIds = []; (window.__flowSeriesIds || []).forEach(id2 => flowIds.push({ id: id2, show: $("d3-layer-flow").checked })); charts["chart-flow3d"].setOption({ series: flowIds }); } });
$("run").onclick = async () => { $("run").disabled = true; $("job-bar").hidden = false; $("job-prog-text").textContent = "作业提交中…"; try { await get("/api/jobs", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ month: $("month").value || (current ? current.month : "2025-01"), reducers: 2, combiner: true }) }); notice("Hadoop 作业已提交。数据清洗与分布式统计可能需要数分钟，完成后自动更新。"); pollJob(); } catch (e) { notice(e.message, true); $("run").disabled = false; } };
async function pollJob() { try { const state = await refreshRuns(); if (state.status === "RUNNING") { $("job-bar").hidden = false; let t = null; if (state.started_at) { t = Math.round((Date.now() - new Date(state.started_at).getTime()) / 1000); } $("job-prog-text").textContent = "MapReduce 计算当前月份全量数据" + (t !== null && t >= 0 ? " · 已运行 " + t + " 秒" : "") + "（进度实时刷新）"; pollTimer = setTimeout(pollJob, 3000); return; } $("job-bar").hidden = true; $("run").disabled = false; if (state.status === "SUCCEEDED") { notice("计算完成，已通过总数守恒检查并发布新结果。"); await refreshAnalysis(); await refreshCluster(); } else if (state.status === "FAILED") { notice("作业失败，保留上一版结果。" + state.error, true); } } catch (e) { notice(e.message, true); $("run").disabled = false; } }
async function loadMonths() { const data = await get("/api/months"); const opts = data.months.map(m => "<option value=\"" + m.month + "\">" + m.month.replace("-", " 年 ") + " 月</option>").join(""); $("month").insertAdjacentHTML("beforeend", opts); $("d3-month").insertAdjacentHTML("beforeend", opts); return data.months; }

/* ========== year ========== */
let yearData = null;
async function renderYear() { if (!yearData) yearData = await get("/api/year"); const stats = yearData.stats.filter(s => s.status === "ok"); const trips = stats.reduce((a, s) => a + s.trips, 0); $("yk-trips").textContent = fmtk(trips); $("yk-daily").textContent = fmtk(trips / 365); $("yk-dist").textContent = fmtk(stats.reduce((a, s) => a + (s.distance_km || 0), 0)); $("yk-rev").textContent = "$" + fmtk(stats.reduce((a, s) => a + (s.revenue_usd || 0), 0)); const months = yearData.months; const mh = yearData.month_hour; const heatMax = Math.max(...mh.flat()); chart("chart-mh").setOption({ grid: { left: 60, right: 20, top: 16, bottom: 34 }, xAxis: { type: "category", data: Array.from({ length: 24 }, (_, i) => i + ":00"), splitArea: { show: true }, axisLabel: { interval: 2 } }, yAxis: { type: "category", data: months.map(m => m.slice(5) + " 月"), splitArea: { show: true } }, visualMap: { min: 0, max: heatMax, calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#eef3fa", "#9dc0ea", "#4c7dd8", "#b0409a", "#8d1f6b"] }, text: ["高", "低"], textStyle: { color: "#666" } }, tooltip: { formatter: function (p2) { return p2.value[1] + " 月 " + p2.value[0] + " · " + fmt(p2.value[2]) + " 次"; } }, series: [{ type: "heatmap", data: mh.flatMap((row, mi) => row.map((v, hi) => [hi, mi, v])), emphasis: { itemStyle: { borderColor: "#333", borderWidth: 1 } }, label: { show: false } }] }); chart("chart-month").setOption({ grid: { left: 46, right: 20, top: 24, bottom: 30 }, xAxis: { type: "category", data: stats.map(s => s.month.slice(5) + " 月") }, yAxis: { type: "value", splitLine: { lineStyle: { color: "#eef2f7" } }, axisLabel: { formatter: v => v } }, tooltip: { trigger: "axis" }, series: [{ type: "bar", data: stats.map(s => +(s.trips / 10000).toFixed(1)), itemStyle: { color: palette.blue, borderRadius: [4, 4, 0, 0] }, label: { show: true, position: "top", fontSize: 10, color: "#3c4a5e" } }] }); const wd = yearData.weekday_hour; const wdMax = Math.max(...wd.flat()); chart("chart-wd").setOption({ grid: { left: 60, right: 20, top: 16, bottom: 34 }, xAxis: { type: "category", data: Array.from({ length: 24 }, (_, i) => i + ":00"), splitArea: { show: true }, axisLabel: { interval: 2 } }, yAxis: { type: "category", data: ["周一", "周二", "周三", "周四", "周五", "周六", "周日"], splitArea: { show: true } }, visualMap: { min: 0, max: wdMax, calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#eef3fa", "#9dc0ea", "#4c7dd8", "#b0409a", "#8d1f6b"] } }, tooltip: { formatter: function (p2) { return p2.value[1] + " · " + p2.value[0] + " · " + fmt(p2.value[2]) + " 次"; } }, series: [{ type: "heatmap", data: wd.flatMap((row, wi) => row.map((v, hi) => [hi, wi, v])), label: { show: false } }] }); const curves = yearData.day_curves; const monthsWith = Object.keys(curves).sort(); const dayLabels = Array.from({ length: 31 }, (_, i) => String(i + 1)); const palette12 = ["#4c7dd8", "#2fa08f", "#e0a33e", "#d8584c", "#8b6fd8", "#4a90a4", "#a86e3f", "#5f9e5f", "#b9679b", "#6f7bb8", "#c56f4c", "#4aa4b8"]; chart("chart-days").setOption({ grid: { left: 44, right: 20, top: 30, bottom: 30 }, xAxis: { type: "category", data: dayLabels, axisLabel: { interval: 2 } }, yAxis: { type: "value", splitLine: { lineStyle: { color: "#eef2f7" } } }, legend: { data: monthsWith.map(m => m.slice(5) + " 月"), textStyle: { fontSize: 10 }, top: 2, type: "scroll", pageIconSize: 10 }, tooltip: { trigger: "axis" }, series: monthsWith.map((m, mi) => ({ name: m.slice(5) + " 月", type: "line", smooth: true, symbol: "none", lineStyle: { width: 1.6, color: palette12[mi % 12] }, itemStyle: { color: palette12[mi % 12] }, data: curves[m].map(pair => [Number(pair[0].slice(8)) - 1, +(pair[1] / 10000).toFixed(2)]) })) }); }

/* ========== od ========== */
function zoneGroup(name) { const n = String(name).toLowerCase(); if (n.includes("airport")) return "机场"; if (/upper|yorkville|lenox|carnegie|lincoln/.test(n)) return "曼哈顿北部"; if (/midtown|times sq|garment|hell|theatre/.test(n)) return "曼哈顿中城"; if (/soho|west village|east village|greenwich|bowery|little italy|chinatown|financial|lower|fulton|gramercy|union sq|chelsea|kips|murray|stuyvesant|madison|penn station/.test(n)) return "曼哈顿其他"; if (n.includes("queens")) return "皇后区"; if (n.includes("brooklyn")) return "布鲁克林"; if (n.includes("bronx")) return "布朗克斯"; if (n.includes("staten")) return "史坦顿岛"; if (n.includes("newark")) return "EWR"; return "其他"; }
const groupColors = { "机场": "#2fa08f", "曼哈顿北部": "#4c7dd8", "曼哈顿中城": "#8b6fd8", "曼哈顿其他": "#5c9bd6", "皇后区": "#d87f3e", "布鲁克林": "#d8584c", "布朗克斯": "#8d6e63", "史坦顿岛": "#7e57c2", "EWR": "#607080", "其他": "#9aa7b5" };
let odData = null;
const bcolors = { Manhattan: "#4c7dd8", Queens: "#2fa08f", Brooklyn: "#e0a33e", Bronx: "#d8584c", "Staten Island": "#8b6fd8", EWR: "#607080" };
async function renderOd() { if (!odData) odData = await get("/api/od"); const brMap = new Map(geo.features.map(f => [f.properties.name, f.properties.borough])); const san = odData.sankey; const nodeColor = {}; san.names.forEach(n => { nodeColor[n] = groupColors[zoneGroup(n)] || "#9aa7b5"; }); chart("chart-sankey").setOption({ tooltip: { trigger: "item", formatter: function (p2) { if (p2.dataType === "edge") return p2.data.source + " → " + p2.data.target + "<br/><strong>" + fmt(p2.data.value) + "</strong> 次"; return p2.name + "<br/>全年" + fmt(p2.value) + " 次"; } }, series: [{ type: "sankey", data: san.names.map(n => ({ name: n, itemStyle: { color: nodeColor[n] } })), links: san.links.map(function (l) { return { source: l[0], target: l[1], value: l[2], lineStyle: { color: nodeColor[l[0]] || "#9aa7b5", opacity: 0.45, curveness: 0.5 } }; }), label: { fontSize: 10.5, color: "#3c4a5e", formatter: function (p2) { return p2.name.length > 14 ? p2.name.slice(0, 13) + "…" : p2.name; } }, nodeWidth: 12, nodeGap: 7, left: 44, right: 44, top: 14, bottom: 14, emphasis: { focus: "adjacency" } }] }); const nameOf = id => nameOfGeo(id); $("odk-pairs").textContent = fmt(odData.top_od.length); const top0 = odData.top_od[0]; $("odk-top").textContent = top0 ? nameOf(top0.origin) + " → " + nameOf(top0.destination) : "—"; const list = $("od-list"); list.replaceChildren(); odData.top_od.slice(0, 12).forEach((pair, i) => { const row = document.createElement("div"); row.className = "rank-row od-row"; const src = nameOf(pair.origin), dst = nameOf(pair.destination); row.innerHTML = "<span class=\"rank-num\">" + String(i + 1).padStart(2, "0") + "</span><div><div class=\"rank-name\"><i class=\"dot\" style=\"background:" + (bcolors[brMap.get(src)] || "#9aa7b5") + "\"></i>" + escapeHtml(src) + "</div><div class=\"rank-sub\">→ " + escapeHtml(dst) + "</div><div class=\"rank-bar\"><span style=\"width:" + (100 * pair.trips / odData.top_od[0].trips) + "%\"></span></div></div><span class=\"rank-value\">" + fmtk(pair.trips) + "</span>"; row.title = src + " → " + dst + " · " + fmt(pair.trips) + " 次"; list.appendChild(row); }); const chips = $("od-legend"); chips.replaceChildren(); const groups = []; san.names.forEach(n => { const g = zoneGroup(n); if (groups.indexOf(g) < 0) groups.push(g); }); groups.forEach(g => { const c = document.createElement("span"); c.className = "chip"; c.innerHTML = "<i style=\"background:" + (groupColors[g] || "#9aa7b5") + "\"></i>" + g; chips.appendChild(c); }); const totalLinks = san.links.reduce((a, l) => a + l[2], 0); const inner = san.links.filter(l => zoneGroup(l[0]) === zoneGroup(l[1])).reduce((a, l) => a + l[2], 0); const share = totalLinks ? Math.round(inner / totalLinks * 100) : 0; const rd = $("od-read"); if (rd) rd.textContent = "怎么看：左侧 = 出发区域，右侧 = 到达区域，带宽 = 全年流量，颜色 = 出发区域组（曼哈顿内部再分：北部/中城/其他）。当前 TOP 25 中 " + share + "% 为同组内部流——出租车需求呈强“就近+区内”结构；绿色为机场走廊（JFK / LaGuardia ↔ 曼哈顿）。"; const ap = odData.airports; chart("chart-airport").setOption({ grid: { left: 52, right: 20, top: 32, bottom: 30 }, legend: { data: ["进港出行", "出港出行"], top: 0 }, tooltip: { trigger: "axis" }, xAxis: { type: "category", data: ap.map(a => a.name), axisLabel: { fontSize: 10 } }, yAxis: { type: "value", name: "千次", splitLine: { lineStyle: { color: "#eef2f7" } } }, series: [{ name: "进港出行", type: "bar", data: ap.map(a => +(a.inbound / 1000).toFixed(1)), itemStyle: { color: palette.teal, borderRadius: [3, 3, 0, 0] }, barMaxWidth: 40, label: { show: true, position: "top", fontSize: 10, color: "#3c4a5e" } }, { name: "出港出行", type: "bar", data: ap.map(a => +(a.outbound / 1000).toFixed(1)), itemStyle: { color: palette.amber, borderRadius: [3, 3, 0, 0] }, barMaxWidth: 40, label: { show: true, position: "top", fontSize: 10, color: "#3c4a5e" } }] }); const note = $("od-note"); if (note) note.textContent = "解读：黄车在机场取车（上车）远多于落客（下车）——机场落客大量由网约车/机场大巴承接；EWR 黄车仅偶尔接客。进港≈落客、出港≈接客。"; const bm = {}; let bmax = 0; odData.top_od.forEach(p => { const b1 = brMap.get(nameOf(p.origin)) || "?"; const b2 = brMap.get(nameOf(p.destination)) || "?"; bm[b1] = bm[b1] || {}; bm[b1][b2] = (bm[b1][b2] || 0) + p.trips; if (bm[b1][b2] > bmax) bmax = bm[b1][b2]; }); const bnames = Object.keys(bm); chart("chart-borough").setOption({ grid: { left: 62, right: 30, top: 20, bottom: 48 }, xAxis: { type: "category", data: bnames, splitArea: { show: true }, axisLabel: { fontSize: 11, margin: 10 } }, yAxis: { type: "category", data: bnames, splitArea: { show: true }, axisLabel: { fontSize: 11, margin: 10 } }, visualMap: { min: 0, max: bmax, calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#fdf6e3", "#f5d9a8", "#e0a33e", "#a86e3f"] }, text: ["高", "低"], textStyle: { color: "#666" } }, tooltip: { formatter: function (p2) { return p2.value[1] + " → " + p2.value[0] + "<br/><strong>" + fmt(p2.value[2]) + "</strong> 次"; } }, series: [{ type: "heatmap", data: bnames.flatMap(r => bnames.map(c => [bnames.indexOf(c), bnames.indexOf(r), bm[r][c] || 0])), label: { show: true, fontSize: 10, color: "#6b4a1c", formatter: function (p2) { const v = p2.value[2]; if (!v) return ""; return v >= 10000 ? (v / 10000).toFixed(1) + "万" : (v >= 1000 ? (v / 1000).toFixed(1) + "k" : String(v)); } } }] }); }

/* ========== 3d ========== */
var demData = null;
function setGLView(sync) { const alpha = +$("d3-alpha").value, beta = +$("d3-beta").value, dist = +$("d3-dist").value; const VIEWC = { "chart-scatter3d": "geo3D", "chart-bar3d": "grid3D", "chart-surface": "grid3D" }; const ids = sync ? ["chart-scatter3d", "chart-bar3d", "chart-surface"] : ["chart-scatter3d"]; ids.forEach(id => { if (charts[id]) { const comp = VIEWC[id] || "grid3D"; const o = {}; o[comp] = { viewControl: { alpha: alpha, beta: beta, distance: dist } }; charts[id].setOption(o); } }); }
function d3Preset() { const v = { iso: [42, 8, 170], top: [88, 2, 190], side: [14, 0, 230] }[$("d3-preset").value] || [42, 8, 170]; $("d3-alpha").value = v[0]; $("d3-beta").value = v[1]; $("d3-dist").value = v[2]; setGLView($("d3-sync").checked); }
function demElev(lons, lats, grid, nx, ny, lon, lat) { let bi = 0, bj = 0, bd = 1e9; for (let i = 0; i < nx; i++) { const dx = Math.abs(lons[i] - lon); if (dx < bd) { bd = dx; bi = i; } } bd = 1e9; for (let j = 0; j < ny; j++) { const dy = Math.abs(lats[j] - lat); if (dy < bd) { bd = dy; bj = j; } } return grid[bj][bi]; }
let __glSmoked = null;
function glSmoke() { if (__glSmoked !== null) return __glSmoked; try { const el = document.createElement("div"); el.style.cssText = "position:fixed;left:-400px;top:-400px;width:120px;height:120px;"; document.body.appendChild(el); const c = echarts.init(el); c.setOption({ grid3D: {}, xAxis3D: {}, yAxis3D: {}, zAxis3D: {}, series: [{ type: "bar3D", data: [[0, 0, 1]] }] }); c.dispose(); el.remove(); __glSmoked = true; } catch (e) { console.error("GeoFlow GL smoke failed:", e); try { document.querySelector("div[data-gl-test]") && document.querySelector("div[data-gl-test]").remove(); } catch (_) { } __glSmoked = false; } return __glSmoked; }
function hasWebGL() { try { const c = document.createElement("canvas"); const ctx = c.getContext("webgl") || c.getContext("experimental-webgl"); return !!ctx; } catch (e) { return false; } }
function webglHint() { try { const c = document.createElement("canvas"); const ctx = c.getContext("webgl") || c.getContext("experimental-webgl"); if (ctx) return null; return "canvas.getContext(webgl) 返回 null"; } catch (e) { return "上下文创建异常: " + e.message; } }
let __flowBoroughs = null;
function renderBoroughFlow() {
  if (odData) { renderBoroughFlowReal(odData, demData); }
}
function renderBoroughFlowReal(odData2, demData) {
  const bc = {};
  geo.features.forEach(f => { const b = f.properties.borough; const c = zoneCenter(f); bc[b] = bc[b] || { x: 0, y: 0, n: 0 }; bc[b].x += c[0]; bc[b].y += c[1]; bc[b].n += 1; });
  Object.keys(bc).forEach(b => { bc[b].x /= bc[b].n; bc[b].y /= bc[b].n; });
  const flows = {}, intra = {}, nodeTot = {};
  (odData2.borough_od || []).forEach(r => { const b1 = r[0], b2 = r[1], c = r[2]; flows[b1] = flows[b1] || {}; flows[b1][b2] = c; if (b1 === b2) { intra[b1] = c; } else { nodeTot[b1] = (nodeTot[b1] || 0) + c; nodeTot[b2] = (nodeTot[b2] || 0) + c; } });
  const srcs = Object.keys(flows).filter(b1 => Object.keys(flows[b1] || {}).some(b2 => b2 !== b1));
  const nodeData = Object.keys(nodeTot).map(b => ({ name: b, value: [bc[b].x, bc[b].y, nodeTot[b]] }));
  window.__flowSeriesIds = srcs.map((b1, i) => "flow-" + i);
  const globalIntra = Object.keys(intra).map(b => b + "·区内" + (intra[b] >= 10000 ? (intra[b] / 10000).toFixed(1) + "万" : fmtk(intra[b])));
  const cap = $("d3-caption"); const extra = " · 数据口径：全区县 45.4M 次全量（含区内：" + globalIntra.join("，") + "）";
  if (cap && cap.textContent.indexOf("全区县") < 0) cap.textContent += extra;
  chart("chart-flow3d").setOption({
    grid: { left: 68, right: 42, top: 44, bottom: 52 },
    xAxis: { type: "value", min: demData.bbox[0], max: demData.bbox[2], name: "经度", nameLocation: "middle", nameGap: 26, axisLabel: { fontSize: 10, margin: 10 } },
    yAxis: { type: "value", min: demData.bbox[1], max: demData.bbox[3], name: "纬度", nameLocation: "middle", nameGap: 34, axisLabel: { fontSize: 10, margin: 10 } },
    legend: { data: srcs, top: 0, textStyle: { fontSize: 10 } },
    tooltip: { formatter: function (p2) { if (p2.seriesType === "lines") { const d = p2.data; return d.from + " → " + d.to + "<br/><strong>" + fmt(d.trips) + "</strong> 次 · 占全区 " + (d.trips / 45400745 * 100).toFixed(1) + "%"; } return p2.name + "<br/>跨区流量 " + fmt(p2.value[2]) + " 次"; } },
    series: srcs.map((b1, i) => ({ id: "flow-" + i, name: b1, type: "lines", coordinateSystem: "cartesian2d", data: Object.keys(flows[b1]).filter(b2 => b2 !== b1).map(b2 => ({ coords: [[bc[b1].x, bc[b1].y], [bc[b2].x, bc[b2].y]], trips: flows[b1][b2], from: b1, to: b2 })), lineStyle: { color: (bcolors[b1] || "#9aa7b5"), width: 2.6, opacity: 0.92, curveness: 0.2 }, effect: { show: true, period: 2.4, trailLength: 0.26, symbol: "arrow", symbolSize: 6 }, zlevel: 2 })).concat([{ type: "effectScatter", coordinateSystem: "cartesian2d", data: nodeData, symbolSize: function (v) { return v[2] ? Math.max(9, Math.sqrt(v[2]) / 260) : 9; }, rippleEffect: { brushType: "stroke", scale: 2.2 }, label: { show: true, position: "top", formatter: function (p2) { return p2.name + (p2.value[2] >= 10000 ? " " + (p2.value[2] / 10000).toFixed(1) + "万" : " " + fmtk(p2.value[2])); }, fontSize: 10.5, color: "#2e3a4c" }, itemStyle: { color: "rgba(76,125,216,0.6)" } }])
  });
}
function renderD3Fallback(yearData, demData, month, zmap, months, valueOf, maxv) {
  const legBox = $("d3-leg"); legBox.replaceChildren();
  const legLevels = [["较低", "#9dc0ea"], ["中", "#4c7dd8"], ["较高", "#8b6fd8"], ["高", "#b0409a"], ["峰值", "#d8584c"]];
  legLevels.forEach(lv => { const c = document.createElement("span"); c.className = "chip"; c.innerHTML = "<i style=\"background:" + lv[1] + "\"></i>" + lv[0]; legBox.appendChild(c); });
  const pts = geo.features.map(f => { const c = zoneCenter(f); return [c[0], c[1], valueOf(f.properties.id)]; });
  const pMax = Math.max.apply(null, pts.map(p => p[2]));
  chart("chart-scatter3d").setOption({ grid: { left: 46, right: 26, top: 24, bottom: 40 }, tooltip: { formatter: function (p2) { return p2.name + " · " + fmt(p2.value[2]) + " 次"; } }, xAxis: { type: "value", name: "经度", min: demData.bbox[0], max: demData.bbox[2], scale: true }, yAxis: { type: "value", name: "纬度", min: demData.bbox[1], max: demData.bbox[3], scale: true }, visualMap: { min: 0, max: pMax, calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#9dc0ea", "#4c7dd8", "#8b6fd8", "#b0409a", "#d8584c"] }, text: ["高", "低"] }, series: [{ type: "scatter", data: pts.map(p => ({ name: nameOfGeoNear(p[0], p[1]), value: p, symbolSize: v => (v[2] ? Math.max(3, Math.sqrt(v[2]) * 0.25) : 3) })), itemStyle: { opacity: 0.8 } }] });
  const zt = {}; yearData.zone_month.forEach(r => { if (!month || r[1] === month) zt[r[0]] = (zt[r[0]] || 0) + r[2]; });
  const top = Object.entries(zt).sort((a, b) => b[1] - a[1]).slice(0, 20).map(e => ({ id: +e[0], name: nameOfGeo(e[0]), v: e[1] }));
  const names = top.map(t => t.name);
  const hd = []; for (let ti = 0; ti < top.length; ti++) for (let hh = 0; hh < 24; hh++) hd.push([names.indexOf(top[ti].name), hh, +(top[ti].v / 24 / 1000).toFixed(2)]);
  chart("chart-bar3d").setOption({ grid: { left: 46, right: 30, top: 20, bottom: 48 }, xAxis: { type: "category", data: names, axisLabel: { rotate: 35, fontSize: 9 } }, yAxis: { type: "category", data: Array.from({ length: 24 }, (_, i) => i + ":00"), axisLabel: { interval: 3, fontSize: 9 } }, visualMap: { min: 0, max: Math.max.apply(null, hd.map(d => d[2])), calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#a9c7ec", "#4c7dd8", "#8b6fd8", "#d8584c"] } }, series: [{ type: "heatmap", data: hd, label: { show: false } }] });
  const wd = yearData.weekday_hour;
  const wd2 = []; for (let wi = 0; wi < 7; wi++) for (let hi = 0; hi < 24; hi++) wd2.push([hi, wi, +(wd[wi][hi] / 1000).toFixed(2)]);
  chart("chart-surface").setOption({ grid: { left: 46, right: 30, top: 20, bottom: 40 }, xAxis: { type: "category", data: Array.from({ length: 24 }, (_, i) => i + ":00"), axisLabel: { interval: 3, fontSize: 9 } }, yAxis: { type: "category", data: ["一", "二", "三", "四", "五", "六", "日"] }, visualMap: { min: 0, max: Math.max.apply(null, wd2.map(d => d[2])), calculable: true, orient: "horizontal", left: "center", bottom: 0, inRange: { color: ["#d6e8f8", "#4c7dd8", "#8b6fd8", "#d8584c"] } }, series: [{ type: "heatmap", data: wd2, label: { show: false } }] });
}
function nameOfGeoNear(lon, lat) { let best = null, bd = 1e9; geo.features.forEach(f => { const c = zoneCenter(f); const d = Math.abs(c[0] - lon) + Math.abs(c[1] - lat); if (d < bd) { bd = d; best = f.properties.name; } }); return best || "?"; }
async function renderD3() {
  if (!yearData) yearData = await get("/api/year");
  if (!demData) demData = await get("/api/dem");
  const month = $("d3-month").value;
  const gl = hasWebGL() && glSmoke();
  $("d3-caption").textContent = (month ? month.replace("-", " 年 ") + " 月" : "全年合计") + " · 千次 · " + (gl ? ("地形底图 " + (demData.attribution || "")) : "2D 模式（GL 初始化异常，详见提示）");
  if (!gl && !window.__glHinted) { window.__glHinted = true; const why = webglHint(); notice("3D 引擎初始化失败：" + (why || "WebGL 上下文或 echarts-gl 组件校验未通过") + "，已切换 2D 平面视图。若 chrome://gpu 显示 WebGL 硬件加速正常，通常是 echarts-gl 与 echarts 版本组合问题（本页已使用配套版本），请 Ctrl+Shift+R 强制刷新后再试。", true); }
  const zmap = {}; yearData.zone_month.forEach(row => { (zmap[row[0]] = zmap[row[0]] || {})[row[1]] = row[2]; });
  const months = yearData.months;
  const valueOf = zid => { if (month) return zmap[zid] ? (zmap[zid][month] || 0) : 0; let t = 0; for (let mi = 0; mi < months.length; mi++) t += zmap[zid] ? (zmap[zid][months[mi]] || 0) : 0; return t; };
  let maxv = 1; geo.features.forEach(f => { const v = valueOf(f.properties.id); if (v > maxv) maxv = v; });
  if (!odData) odData = await get("/api/od");
  renderBoroughFlow();
  if (!gl) { renderD3Fallback(yearData, demData, month, zmap, months, valueOf, maxv); return; }
  const nx = demData.lons.length, ny = demData.lats.length;
  const surf = []; for (let j = 0; j < ny; j++) for (let i = 0; i < nx; i++) surf.push([demData.lons[i], demData.lats[j], demData.grid[j][i]]);
  const demPts = [];
  for (let j = 0; j < ny; j++) for (let i = 0; i < nx; i++) { const e = demData.grid[j][i]; demPts.push({ value: [demData.lons[i], demData.lats[j], Math.round(e * 0.02 * 100) / 100], itemStyle: { color: e <= 1.5 ? "#cfdcea" : e <= 45 ? "#b9d9b3" : e <= 120 ? "#a9c9a0" : e <= 220 ? "#c9b791" : "#b29a74" } }); }
  const elevOf = (lon, lat) => demElev(demData.lons, demData.lats, demData.grid, nx, ny, lon, lat);
  if (!geo._mapped3) { echarts.registerMap("nyc", geo); geo._mapped3 = true; }
  const zoneBars = geo.features.map(f => [f.properties.name, valueOf(f.properties.id)]);
  const colTrips = t => { const r = Math.min(1, t / maxv); if (r < 0.25) return "rgb(157,192,234)"; if (r < 0.5) return "rgb(76,125,216)"; if (r < 0.75) return "rgb(139,111,216)"; if (r < 0.92) return "rgb(176,64,154)"; return "rgb(216,88,76)"; };
  try {
    if (!odData) odData = await get("/api/od");
    const towers = geo.features.map(f => { const c = zoneCenter(f); const v = valueOf(f.properties.id); const e = elevOf(c[0], c[1]); return { name: f.properties.name, value: [c[0], c[1], Math.round((e * 0.3 + v / maxv * 55) * 10) / 10], itemStyle: { color: colTrips(v), opacity: 0.82 } }; });
    let __glErrShown = false;
    const safeGL = (fn) => { try { fn(); } catch (e2) { console.error("GeoFlow GL chart:", e2); if (!__glErrShown) { __glErrShown = true; $("d3-caption").textContent = "城市图渲染出错：" + (e2.message || String(e2)) + "（F12 可查 GeoFlow GL chart）"; } } };
    safeGL(() => { chart("chart-scatter3d").setOption({
      tooltip: { formatter: function (p2) { if (p2.seriesType === "bar3D") return p2.value[0] + "<br/><strong>" + fmtk(p2.value[1]) + "</strong> 次 (全年)"; return p2.name + " · 高程 " + p2.value[2] + "m"; } },
      visualMap: { min: 0, max: maxv, dimension: 1, seriesIndex: 1, inRange: { color: ["#9dc0ea", "#4c7dd8", "#8b6fd8", "#b0409a", "#d8584c"] }, text: ["高", "低"], orient: "horizontal", left: "center", bottom: 4, textStyle: { color: "#666" }, calculable: true },
      geo3D: { map: "nyc", boxWidth: 150, boxDepth: 115, regionHeight: 0.9, itemStyle: { color: "#eef2f7", opacity: 0.92, borderColor: "#9db4cd" }, label: { show: false }, emphasis: { label: { show: false } }, viewControl: { alpha: 52, beta: 10, distance: 170 } },
      series: [
        { id: "dem", type: "scatter3D", coordinateSystem: "geo3D", data: demPts, symbolSize: 1.8, label: { show: false } },
        { id: "towers", type: "bar3D", coordinateSystem: "geo3D", data: zoneBars, barSize: 1.1, shading: "lambert", itemStyle: { opacity: 0.92 }, label: { show: false }, emphasis: { label: { show: true, formatter: function (p2) { return p2.value[0] + " " + fmtk(p2.value[1]); } } } }
      ] }); });
    const zt = {}; yearData.zone_month.forEach(r => { if (!month || r[1] === month) zt[r[0]] = (zt[r[0]] || 0) + r[2]; });
    const top = Object.entries(zt).sort((a, b) => b[1] - a[1]).slice(0, 20).map(e => ({ id: +e[0], name: nameOfGeo(e[0]), v: e[1] }));
    const names = top.map(t => t.name);
    const bdata = []; for (let ti = 0; ti < top.length; ti++) for (let hh = 0; hh < 24; hh++) bdata.push([names.indexOf(top[ti].name), hh, +(top[ti].v / 24 / 1000).toFixed(2)]);
    const bmax = Math.max.apply(null, bdata.map(d => d[2]));
    chart("chart-bar3d").setOption({ grid3D: { boxWidth: 170, boxDepth: 70, viewControl: { alpha: 42, beta: 18, distance: 200 }, axisLabel: { fontSize: 9 } }, xAxis3D: { type: "category", data: names, axisLabel: { interval: 1, rotate: 35, fontSize: 9 } }, yAxis3D: { type: "category", data: Array.from({ length: 24 }, (_, i) => i + ":00"), axisLabel: { interval: 3, fontSize: 9 } }, zAxis3D: { type: "value", name: "千次", axisLabel: { fontSize: 9 } }, visualMap: { min: 0, max: bmax, inRange: { color: ["#a9c7ec", "#4c7dd8", "#8b6fd8", "#b0409a", "#d8584c"] }, text: ["高", "低"], orient: "horizontal", left: "center", bottom: 4, textStyle: { color: "#666" }, calculable: true }, series: [{ type: "bar3D", data: bdata, shading: "lambert", itemStyle: { opacity: 0.88 } }] });
    const wd = yearData.weekday_hour;
    const sdata = []; for (let wi = 0; wi < 7; wi++) for (let hi = 0; hi < 24; hi++) sdata.push([hi, wi, +(wd[wi][hi] / 1000).toFixed(2)]);
    const sMax = Math.max.apply(null, sdata.map(d => d[2]));
    chart("chart-surface").setOption({ grid3D: { boxWidth: 130, boxDepth: 95, viewControl: { alpha: 38, beta: 22, distance: 190 } }, xAxis3D: { type: "value", name: "小时", min: 0, max: 23, axisLabel: { fontSize: 9 } }, yAxis3D: { type: "value", name: "星期", min: 0, max: 6, axisLabel: { formatter: function (v) { return ["周一", "周二", "周三", "周四", "周五", "周六", "周日"][Math.round(v)]; }, fontSize: 9 } }, zAxis3D: { type: "value", name: "千次", axisLabel: { fontSize: 9 } }, visualMap: { min: 0, max: sMax, inRange: { color: ["#d6e8f8", "#4c7dd8", "#8b6fd8", "#d8584c"] }, text: ["高", "低"], orient: "horizontal", left: "center", bottom: 4, textStyle: { color: "#666" }, calculable: true }, series: [{ type: "surface", data: sdata, shading: "lambert", wireframe: { show: false } }] });
    const legBox = $("d3-leg"); legBox.replaceChildren();
    const legLevels = [["较低", "#9dc0ea"], ["中", "#4c7dd8"], ["较高", "#8b6fd8"], ["高", "#b0409a"], ["峰值", "#d8584c"]];
    legLevels.forEach(lv => { const c = document.createElement("span"); c.className = "chip"; c.innerHTML = "<i style=\"background:" + lv[1] + "\"></i>" + lv[0] + "(" + (month ? "该月" : "全年") + "≤" + fmtk(maxv) + ")"; legBox.appendChild(c); });

    setGLView($("d3-sync").checked);
  } catch (e) {
    console.error("GeoFlow 3D:", e);
    $("d3-caption").textContent = "3D 渲染出错：" + e.message + "（请把 F12 控制台信息发给我）";
    notice("3D 渲染出错：" + e.message, true);
  }
}
function findZoneId(name) { const f = geo.features.find(x => x.properties.name === name); return f ? f.properties.id : 0; }

/* ========== forecast ========== */
let fcData = null, fcCurve = null, fcEvidence = null;
async function renderForecast() { const evidence = fcEvidence || (await get("/api/evidence")).forecast; fcEvidence = evidence; if (!fcData) fcData = await get("/api/forecast"); if (!fcCurve) fcCurve = await get("/api/forecast-curve"); const mm = fcData.metrics, bm = mm.baseline_lag7; const gain = (1 - mm.model.rmse / bm.rmse) * 100; $("fc-model").textContent = fcData.model; $("fc-rmse").textContent = mm.model.rmse; $("fc-mape").textContent = mm.model.mape_pct + "%"; $("fc-gain").textContent = (gain >= 0 ? "+" : "") + gain.toFixed(1) + "%"; const err = new Map(fcData.zones.map(z => [z.zone, z])); const dirs = zoneDirs(geo); const fm = $("fc-map"); fm.replaceChildren(); for (const f of geo.features) { const path = document.createElementNS("http://www.w3.org/2000/svg", "path"); path.setAttribute("d", zonePath(f, dirs)); path.setAttribute("fill-rule", "evenodd"); const info = err.get(f.properties.id); const pct = info ? Math.max(-50, Math.min(50, info.error_pct)) : 0; const t = pct / 50; let fill; if (pct === 0) fill = "#e9edf3"; else if (pct > 0) fill = "rgb(" + Math.round(232 - 90 * t) + "," + Math.round(236 - 60 * t) + "," + Math.round(242 - 120 * t) + ")"; else fill = "rgb(" + Math.round(216 + 36 * t) + "," + Math.round(224 + 10 * t) + "," + Math.round(248 - 4 * t) + ")"; path.setAttribute("fill", fill); const title = document.createElementNS("http://www.w3.org/2000/svg", "title"); title.textContent = info ? info.name + ": 预测 " + fmtk(info.predicted) + " / 实际 " + fmtk(info.actual) + " · 误差 " + info.error_pct.toFixed(1) + "%" : f.properties.name; path.appendChild(title); fm.appendChild(path); } chart("chart-fc-curve").setOption({ grid: { left: 50, right: 18, top: 28, bottom: 52 }, legend: { data: ["实际", "预测"], top: 0, textStyle: { fontSize: 10 } }, tooltip: { trigger: "axis" }, dataZoom: [{ type: "inside", start: 0, end: 13 }, { type: "slider", bottom: 0, height: 18 }], xAxis: { type: "time", axisLabel: { formatter: function (v) { const d = new Date(v); return (d.getMonth() + 1) + "-" + String(d.getDate()).padStart(2, "0") + " " + String(d.getHours()).padStart(2, "0") + ":00"; } } }, yAxis: { type: "value", splitLine: { lineStyle: { color: "#eef2f7" } } }, series: [{ name: "实际", type: "line", symbol: "none", sampling: "lttb", data: fcCurve.points.map(p => [new Date(p[0]).getTime() + 8 * 3600e3, p[1]]), lineStyle: { color: palette.blue, width: 1.2 } }, { name: "预测", type: "line", symbol: "none", sampling: "lttb", data: fcCurve.points.map(p => [new Date(p[0]).getTime() + 8 * 3600e3, p[2]]), lineStyle: { color: palette.red, width: 1.2 } }] }); const tabs = (fcEvidence.weather_effect && fcEvidence.weather_effect.temp_bins) || []; const wet = (fcEvidence.weather_effect && fcEvidence.weather_effect.precip_vs_dry) || {}; const cats = tabs.map(t => t.range); cats.push("晴(无降水)"); cats.push("有降水"); const vals = tabs.map(t => t.mean); vals.push(wet.dry_mean || 0); vals.push(wet.wet_mean || 0); chart("chart-weather").setOption({ grid: { left: 42, right: 16, top: 26, bottom: 28 }, xAxis: { type: "category", data: cats, axisLabel: { fontSize: 10 } }, yAxis: { type: "value", splitLine: { lineStyle: { color: "#eef2f7" } } }, tooltip: { trigger: "axis" }, series: [{ type: "bar", data: vals, itemStyle: { color: function (p2) { return p2.dataIndex === cats.length - 1 ? palette.teal : palette.amber; }, borderRadius: [3, 3, 0, 0] }, barMaxWidth: 44, label: { show: true, position: "top", fontSize: 10, color: "#3c4a5e" } }] }); const imp = (fcEvidence.weather_effect && fcEvidence.weather_effect.feature_importance) || {}; const rows = Object.entries(imp).slice(0, 10); const maxI = rows.length ? rows[0][1] : 1; const impBox = $("fc-importance"); impBox.replaceChildren(); const impColors = ["#4c7dd8", "#5b87d9", "#6a91da", "#789bdb", "#87a5dc", "#96afdd", "#a5b9de", "#b4c3df", "#c3cde0", "#d2d7e1"]; rows.forEach((kv, i) => { const row = document.createElement("div"); row.className = "rank-row imp-row"; row.innerHTML = "<span class=\"rank-num\">" + (i + 1) + "</span><div><div class=\"rank-name\">" + escapeHtml(kv[0]) + "</div><div class=\"rank-bar\"><span style=\"width:" + (100 * kv[1] / maxI) + "%;background:" + impColors[i] + "\"></span></div></div><span class=\"rank-value\">" + kv[1] + "</span>"; impBox.appendChild(row); }); }

/* ========== init ========== */
const HELP_HTML = "<div class=\"help-card\"><button class=\"close\" id=\"help-close\">✕ 关闭</button><h3>核心指标</h3><ul><li><code>有效出行</code>：通过全部质量规则的记录条数（本平台的需求代理指标）。质量规则：字段完整、有限数、月份匹配、区域 ID 1–263、时长 1–180 分钟、距离 0–100 英里、金额 0–1000 美元。</li><li><code>规则排除</code>：被剔除的原始记录条数及原因（如 InvalidDistance / InvalidAmount / UnknownZone），有效率 = 有效 / 原始。</li><li><code>营收</code>：全部有效行程的 total_amount 合计（美元，含小费前的结算金额）。</li><li><code>总里程 / 平均距离 / 平均时长 / 平均金额</code>：由 trip_distance、起止时间差、total_amount 按区域和时间聚合的加权均值（整数定点求和保证精确）。</li></ul><h3>筛选口径</h3><ul><li><code>月份</code>：单月全量（最新默认）；<code>行政区</code>：按 pickup 区域所属行政区；<code>小时</code>：pickup 小时（24 = 全天）。</li></ul><h3>2D 图表</h3><ul><li><code>空间分布(总览)</code>：区域上车量色阶图，点击查看该区域在当前口径下的统计。</li><li><code>小时分布趋势</code>：全天 24 小时出行量曲线（不随小时筛选变化，只随行政区/月份变化）。</li><li><code>月份×小时热力图 / 星期×小时热力图</code>：全年节律；颜色越深需求越高。</li><li><code>月度总量 / 月度日均曲线</code>：月度规模与每日节奏（12 条线，每条 31 个日点）。</li><li><code>OD 桑基图</code>：左列=出发区域、右列=到达区域，带宽=全年流量，颜色=出发区域组（曼哈顿再分北部/中城/其他）；<strong>曼哈顿区内流占主导是真实结构</strong>（通勤与短程）。</li><li><code>机场进出</code>：落客/接客量——黄车机场接客远多于落客：落客多由网约车/机场大巴承接，EWR 黄车几乎不接客。</li><li><code>行政区 OD 矩阵</code>：区域间流量热矩阵，色深=流量，空格=无 TOP 记录。</li><li><code>预测误差图</code>：12 月预测 vs 实际的区域误差（红=低估、蓝=高估、灰=接近）；<code>预测曲线</code>：榜首区域逐小时实际 vs 预测（可拖动缩放）。</li><li><code>天气影响</code>：温度区间/降水的平均需求差异；<code>特征重要性</code>：LightGBM 增益——hour/weekday/滞后>天气>区域。</li></ul><h3>3D 图表</h3><ul><li><code>城市需求立体映射</code>：三层可勾选——DEM 地形（Mapbox/OSM 高程，绿→沙按米）、需求柱体（颜色=出行量五档，柱高=标准化的量）、OD 交通流（动态弧线=TOP30 流向）；可拖动旋转/缩放/方向同步。</li><li><code>区域×小时 3D 矩阵</code>：TOP20 区域 × 24 小时的量（色带=千次）。</li><li><code>星期×小时 3D 曲面</code>：全年周节律地形。</li></ul><h3>预测指标</h3><ul><li><code>RMSE / MAE</code>：绝对误差的均方根/平均绝对值（原始出行数单位，越小越好）。</li><li><code>MAPE</code>：平均绝对百分比误差（稀疏单元会拉高）；<code>高需求 MAPE</code>：仅 actual≥5 的单元，更贴近运营关注。</li><li><code>基线</code>：季节性朴素——上周同期观测值；模型相对基线的改善百分比。</li><li>口径：按月严格切分（1–10 训练 / 11 验证 / 12 测试），落后特征用观测值（单步回测）。</li></ul></div>";
function openHelp() { const ov = document.createElement("div"); ov.className = "help-overlay"; ov.innerHTML = HELP_HTML; document.body.appendChild(ov); const close = () => ov.remove(); ov.onclick = e => { if (e.target === ov) close(); }; ov.querySelector("#help-close").onclick = close; }
$("help-btn").onclick = openHelp;
async function init() { try { geo = await get("/api/zones"); buildMap(); await loadMonths(); await Promise.all([refreshAnalysis(), refreshCluster()]); const state = await refreshRuns(); if (state.status === "RUNNING") { $("run").disabled = true; notice("已有 Hadoop 作业正在执行。"); pollJob(); } attachZoom(); } catch (e) { notice(e.message, true); } }
init();
